apiVersion: v1
kind: Service

metadata:
  name: transaction-api
  namespace: prod

spec:
  selector:
    app: transaction-api

  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080

  type: LoadBalancer
